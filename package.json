{
  "name": "sentinel",
  "version": "1.0.0",
  "description": "Agentic credit-card resolution platform, built with Flue",
  "scripts": {
    "dev": "flue dev --target node",
    "build": "flue build --target node",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "chat": "flue run chat-turn --target node --payload",
    "start": "flue build --target node && PORT=3583 node --env-file=.env dist/server.mjs",
    "serve": "node --env-file=.env dist/server.mjs",
    "data:generate": "node scripts/generate-data.mjs",
    "data:seed": "node --env-file=.env scripts/seed-supabase.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@flue/postgres": "^0.11.0",
    "@flue/runtime": "^0.11.0",
    "@supabase/supabase-js": "^2.108.1",
    "hono": "^4.12.25",
    "valibot": "^1.4.1"
  },
  "devDependencies": {
    "@flue/cli": "^0.11.0",
    "@types/node": "^25.9.2",
    "typescript": "^6.0.3"
  }
}
